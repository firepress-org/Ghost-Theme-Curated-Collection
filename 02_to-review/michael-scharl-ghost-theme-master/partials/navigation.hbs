<nav class="ms-nav">
    <div class="container">
        <ul class="ms-nav__list">
            {{!--<li class="ms-nav__item ms-nav__item--text-logo">
                <h1 class="sr-only">{{@blog.title}}</h1>
                <img src="{{asset "img/header.svg"}}" alt="{{@blog.title}}">
            </li>--}}
            {{#if @blog.logo}}
                <li class="ms-nav__item ms-nav__item--logo">
                    <a href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}"/></a>
                    <h1 class="sr-only">{{@blog.title}}</h1>
                </li>
            {{/if}}
            {{#if @blog.navigation}}
                {{#foreach navigation}}
                    <li class="ms-nav__item ms-nav__item--{{slug}}{{#if current}} ms-nav__item--current{{/if}}">
                        <a href="{{url absolute="true"}}"><span>{{label}}</span></a>
                    </li>
                {{/foreach}}
            {{/if}}
            <li class="ms-nav__item ms-nav__item--subscribe">
                <a href="{{@blog.url}}/rss/"><span>Subscribe</span></a>
            </li>
        </ul>
    </div>
</nav>
